html
    head
        title Detail
        p P OYDUM OY
        table#table

    body
        script(src='https://code.jquery.com/jquery-3.3.1.min.js')
        script.
            var url = "https://yts.am/api/v2/list_movies.json";
            var searchUrl = "?query_term="

            var url = window.location.search
            url = url.replace("?id=", "");
            url = url.replace("%20", " ");
            url = url.replace("&page=", " ");
            page = url.match(/\d+$/);

            title = url.replace(page, "")
            //console.log("title: " + title)


            $.post('/detail/detail', {url: url})
                .done(function (ret) {
                    console.log(ret)
                    if (ret.success) {
                        $("#table").empty()
                        //$("#table").append('<tr><th>#</th><th style="padding-left: 20px">Cover</th><th style="padding-left: 20px">Movie Names</th><th style="padding-left: 20px">Year</th><th style="padding-left: 20px">Rating</th></tr>')
                        console.log(ret.body)
                    }
                })